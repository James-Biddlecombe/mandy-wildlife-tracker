#pragma once

// ============================================================
// Mandy Wildlife — secrets template
// ------------------------------------------------------------
// Copy this file to "secrets.h" and fill in your real values:
//
//     cp secrets.example.h secrets.h
//
// secrets.h is git-ignored (see .gitignore) so your real
// credentials never get committed. This template file is safe
// to commit because it contains only placeholders.
// ============================================================

// Your Wi-Fi network name and password.
constexpr char WIFI_SSID[]     = "YOUR_WIFI";
constexpr char WIFI_PASSWORD[] = "YOUR_PASSWORD";
